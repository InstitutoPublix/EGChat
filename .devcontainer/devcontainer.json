{
  "name": "mentor-virtual-tjce - Flask App",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "app.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-python.debugpy"
      ]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y <packages.txt; [ -f requirements.txt ] && pip3 install --user -r requirements.txt; echo '✅ Packages installed and Requirements met'",
  "postAttachCommand": {
    "server": "python -m flask run --host=0.0.0.0 --port=8080"
  },
  "portsAttributes": {
    "8080": {
      "label": "Flask Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8080
  ]
}